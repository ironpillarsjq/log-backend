# env.conf - 针对不同平台/机器，只需要改这个文件

# 1后端服务器地址
# @SET SERVER_HOST=100.107.214.72
@SET SERVER_HOST=localhost
@SET SERVER_PORT=8080

@SET FLB_BUFFER_PATH=D:\Major\fluent-bit\buffer
@SET FLB_DB_PATH=D:\Major\fluent-bit\winlog.db

@INCLUDE ../windows/log_transfer.conf