# env.conf - 针对不同平台/机器，只需要改这个文件

# 1后端服务器地址
@SET SERVER_HOST=100.107.214.72
#@SET SERVER_HOST=localhost
@SET SERVER_PORT=8080

@SET FLB_BUFFER_PATH=/var/lib/fluent-bit/storage

@INCLUDE log_transfer.conf
#@INCLUDE heartbeat.conf